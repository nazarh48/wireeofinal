config:
  target: "http://127.0.0.1:5000"
  phases:
    - duration: 60
      arrivalRate: 10
      name: "ranges-endpoint"
  defaults:
    headers:
      Accept: "application/json"

scenarios:
  - name: "Public ranges listing"
    flow:
      - get:
          url: "/api/ranges?status=active"

